# React Router Demo App

A React app demonstrating React Router v6 with nested routes, a left sidebar, and dynamic content. Features pages like Home, About, Contact, Posts (fetching data from JSONPlaceholder), and Add List, with SPA navigation and clean layout.
